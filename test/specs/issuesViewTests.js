define(function(require) {
    var models = require('../../app/scripts/main');

    describe('Test the issues fetcher', function () {
        it('should run here few assertions', function () {
            assert(true);
        });
    });
});
